.clearfix
%footer
  .row
    .col-sm-5
      = "#{Time.now.year} machine languages"
    .col-sm-7.text-right
      - if current_user.present? && !current_user.new_record?
        = link_to 'logout', destroy_user_session_path
      - else
        = link_to 'admin login', new_user_session_path

